"""Side B API route modules.

Route modules are registered in ``api_server.py``.
Each route module exposes a function ``register_routes(app)``
that attaches FastAPI endpoints to the application instance.
"""
